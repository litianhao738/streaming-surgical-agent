# 中断试验两帧的事后离线描述结果

用户要求查看已跑结果后，使用现有缓存与 Training 标签补算；新增 API 请求为 0。此前技术报告未评估 GT 的描述对应补算之前。本文件不替代冻结的 64 帧评估，不用于修改抽样、提示或默认方案。

| 完整配对帧 | 无 Tracker 五头平均 F1 | 有 Tracker 五头平均 F1 | FP+FN | 回放完整路径调用数 |
|---|---:|---:|---|---|
| VID103_5626 | 20.00% | 20.00% | 14 → 14 | 10 → 9 |
| VID23_501 | 72.00% | 77.33% | 7 → 6 | 8 → 10 |

两帧合并计数后按五个任务分别计算 F1 再平均：无 Tracker 为 47.78%，有 Tracker 为 51.33%；FP+FN 从 21 降到 20。该数不是上表逐帧 F1 的简单平均。抓/牵拉合并口径为 52.22% → 55.78%，错误数 19 → 18。

VID103 的最终标签完全不变，但可提前少问一位审核员。VID23 的最终 target 标签增加 ID 10，其余四个任务预测不变；按标签核对减少一个漏检，但多问两位审核员。两帧合计回放调用数为 18 → 19；它包含复用缓存对应的完整推理路径，不等于本轮实际新发请求数。

VID23 对照组的 Gemini 回答未通过 JSON 校验，而 Tracker 组通过。现有规则会将无效回答纳入回退与提前停止判断，所以这一帧的差异同时涉及回答有效性，不能从单次配对推断为轨迹证据的稳定语义收益。

另外两个目标只有对照组：VID31_12001 F1 为 50.00%、FP+FN 为 11；VID96_5901 F1 为 48.00%、FP+FN 为 12（包含 GLM API 失败后的既有回退）。二者没有 Tracker 对照，不纳入上述配对统计。

这两个完整配对各组均实际采集 8 次后续回答，用于因果顺序回放。两组的 OpenRouter 采集费用分别为 $0.0121332 / $0.0122238，各自另有 GLM 2 次和 DeepSeek 2 次；全部四个已开始目标总费用仍为 $0.03471715，GLM/DeepSeek 各 6 次。采集包括回放时未必需要的后续回答，不能将采集账单直接当作提前停止后的推理成本。

结论仅限这两帧：一帧结果不变、一帧少漏一个目标，总调用数增加一次。样本不足且存在格式失败差异，尚未证明同时降低成本并保持精度稳定。保持原默认及停止状态，无后续付费采集。

完整数值及逐帧预测：`artifacts/training/gate/tracker_review_evidence_20260914_r2/partial_descriptive_results_20260914.json`。
