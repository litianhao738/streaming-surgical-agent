# 方案 5、6 采集前准备

尚未发出付费请求。先执行方案 5，再执行方案 6；16 个 Training 样本来自四个视频各四个时间分位，不按 GT 或 Gate 结果筛选。

方案 5 为原先验与 Tracker 扩展先验的配对；方案 6 只改变 Qwen 初审提示，复用本轮对照的 proposal 和另外四席回复。H0 和阶段历史均固定。主报告比较相同 M1/M2 与阶段后处理后的结果，并另报原始四头结果。输入改变后原 Gate 仅作迁移对照。

请求上限 208：proposal 32、Qwen 48、GPT 32、Gemini 32、GLM 32、DeepSeek 32。拟批准的账户上限：OpenRouter $5、阿里云 Qwen ¥6、GLM 32 次、DeepSeek 32 次。GLM/DeepSeek 是请求数量上限，不代表免费。

首次预览的阿里云保守预留合计约 ¥5.43，因此原拟 ¥1 不足以支持完整保守预留；在零请求状态下将待批准计划修订为 ¥6。OpenRouter 预览保守预留约 $4.40。实际新候选池可能改变 token 数，每次请求仍受账户预算约束；预算停止不自动续跑。r1/r2 为失败的离线准备，r3 为原预算预览，r4 是修订后的待批准计划。

16 个样本中，仅 VID96_23151 的方案 5 先验提示实际改变。这个小试验对方案 5 的有效样本很少，不能据其无提升断言方案 5 无效，也不能把两次相同请求的随机差异称为 Tracker 增益。报告必须单列提示改变子集。方案 6 的 Qwen 提示则逐帧改变。

r4 保存修订预算的预览；r5 再冻结提示改变子集的评分输出和本文，作为最终待批准计划。

不读取 VID110/Testing；不更改默认版本；不自动扩大采样。只有用户批准上述付费范围后才执行 collect --allow-paid。
