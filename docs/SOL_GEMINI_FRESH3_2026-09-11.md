# Sol 与 Gemini：三个新 Training 样本

主线 `prior-gated-joint-mainline-v1.0.0`、五头审核模型、提示和 low 推理强度保持不变。对比基座 `openai/gpt-5.6-sol`（OpenAI 严格路由）与 `google/gemini-3.8-flash`（Google AI Studio 严格路由），同目标两模型并行，每模型每目标最多 13 次调用，不自动重试。

预先固定 VID103_14326、VID23_9551、VID31_32601：各视频四分之一时间附近，所有标签掩码有效，完整三帧因果输入，距归档 preflight 计划中已选帧超过 175 帧。选择未使用标签取值或预测结果。先验复用原先按视频留一的 Training 先验，排除整个查询视频；评分真值在预测封存后关联。新三目标单列，未替换原六目标或为求胜继续换样本。

## 固定三个目标的结果

| 基座 | 完成数 | H0 五头平均 F1 | 最终五头平均 F1 | 最终 IVT F1 | Phase 正确数 |
|---|---:|---:|---:|---:|---:|
| Sol | 2/3 | 39.44 | 47.32 | 44.44 | 0/3 |
| Gemini | 3/3 | 75.45 | 77.69 | 46.15 | 3/3 |

Sol 的 VID103_14326 在 H0 被图片审核 403 拒绝，提示 violence/graphic，服务商明确表示不扣费。此目标按既定规则以空预测评分。因此上表包含服务可用性损失，不能完全解释为识别能力差距。其余 26 次 Sol 请求及 Gemini 39 次请求均成功解析，没有追加重试。

## 共同成功的两个目标：仅用于诊断

| 基座 | H0 平均 F1 | 最终平均 F1 | 最终 IVT F1 | 最终 Phase 正确数 | 最终总错漏 |
|---|---:|---:|---:|---:|---:|
| Sol | 54.67 | 63.81 | 66.67 | 0/2 | 9 |
| Gemini | 74.67 | 70.86 | 28.57 | 2/2 | 9 |

该子集为 VID23_9551、VID31_32601，不替代固定三目标结果。两者 H0 IVT F1 同为 33.33；Sol 经主线审核后 IVT 改善，而 Gemini 的 IVT 变差。Sol 两个目标的 H0 和最终 Phase 都是 3，真值都是 1；Gemini 均为 1。原始响应重放与独立 TP/FP/FN 复算均通过。

结论应细化：本次 Sol 在成功样本的最终 IVT 上更好，不能说它所有识别项都更弱；但 Phase 错误及接口拒绝使其在当前完整任务上仍未胜出。三样本不足以认定通用模型能力优劣，且新三个目标真值 Phase 恰好全部为 1，不能推广为全面 Phase 能力评估。当前证据尚不足以将 Gemini 主线基座替换为 Sol。若继续研究，应预先固定更大的分层样本与推理强度对照，而不是不断更换小样本直到某模型胜出。

## 时间、费用与归档

共 66 次实际调用，双模型并行、目标依次执行，API 执行墙钟时间 120.14 秒，不含准备和审计。

| 模型 | 调用数 | 各目标耗时之和 | OpenRouter 原生费用 | 阿里云保守估算 |
|---|---:|---:|---:|---:|
| Sol | 27 | 78.90 秒 | $0.08186212 | ¥0.052621 |
| Gemini | 39 | 93.80 秒 | $0.08195593 | ¥0.082606 |
| 总计 | 66 | 不将并行时间相加 | $0.16381805 | ¥0.135227 |

Sol 仅完成两个完整目标，上表总费用不能直接当作等工作量的价格对比。原始账本对 403 保留未知预占，独立审计根据服务商明确的不扣费声明将其计为零，未覆盖原账本。

档案：`artifacts/preflight/sol_gemini_fresh3_20260911_v1/`，包含 plan、preflight、frozen_runner、全部原始请求/响应、completion 证据哈希、comparison、scored_truth、independent_audit、common_success_metrics。准备阶段的模拟请求除模型/路由和 Sol 不支持的 temperature 外配对一致；所有 66 次实际请求通过零调用重放。根目录 DEFAULT、BEST、TRAINING_BASE_MODEL_SELECTION 哈希均未变。未启动四视频全量训练数据或 Gate 采集。

入口：`scripts/compare_sol_gemini_fresh3.py`；独立审计：`tools/audit/audit_sol_gemini_fresh3.py`。存在归档时入口拒绝重复运行，避免意外重复付费。
