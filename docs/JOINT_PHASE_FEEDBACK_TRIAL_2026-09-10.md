# Phase合入图谱分支：当前五席联合评分与第二轮反馈修复

用户授权运行标注流程，要求只微调Verifier/Repair机制或prompt，调整要有依据并记录。默认读取于2026-09-10：`parallel-phase-repair-v1.3.0-glm-low`。本实验独立，不自动改默认、不修改其他会话文件，不训练Gate。

**已完成。** 最终新16目标上，默认/联合第一轮/替代候选第二轮的五头均分分别为62.66/65.30/65.54，总标签错误分别为93/77/78。总体收益已通过预定标准；建议优先扩大验证第一轮，第二轮虽IVT更高，但相对第一轮修对1项、改错2项。最终确认、所有失败变体、费用与限制详见本文后半部分。默认未切换，所有后台任务已结束。

## 冻结范围

五席固定为GLM `z-ai/glm-5.3-flash`（Together，low，exclude仅隐藏推理）、Qwen `qwen3.5-35b-a3b`（阿里云，thinking=false）、GPT `openai/gpt-5.6-luna`（none）、Gemini `google/gemini-3.5-flash-lite`（minimal）、DeepSeek `deepseek/deepseek-v4-flash-vision-exp`（关闭推理）。不以旧席位键grok误称实际模型。

开发8目标按原32目标每视频第3、第6项选定：VID103/17876、33126；VID23/13751、25951；VID31/40251、73251；VID96/14601、24776。它们已经用于历史开发，不是独立验证。各组共享封存H0（历史Gemini协议）、三帧[-2,-1,0]秒、图谱提示和初始四头候选。没有为追分修改H0、原补候选prompt、图片或Training先验。

A：用当前五席重新运行默认四头均分和独立Phase多数票，两分支并行。
B：新增一次Repair模型的Phase推荐；只推荐阶段，不修改初始四头候选；Python加入全部七阶段。五模型每席一次完成四头候选和七阶段审核。复用原四头精简契约，补上阶段定义、当前预测假设及不可信Phase推荐。五头各自算分，不跨头平均接纳。
C：从B同一份第一轮结果出发；存在四头未解决问题、阶段支持不足、多个阶段竞争或阶段面板不完整时，把具体观察与疑点交给Repair。Repair可扩充关系假设及推荐阶段，全部七阶段始终保留；五模型重新审核该池，以当轮五份有效评分接纳。最多第二轮，不拼接旧票、不重复请求凑多数。

第一轮推荐及第二轮Repair沿用Gemini补候选模型和生成配置，但其职责明确属于新增Repair机制。故A/B不是纯粹只改均分公式的单因素消融；B/C才能观察这条联合流程第二轮的增量。第二轮即使候选ID未变，只要有既定疑点和修复假设仍可复核；最终状态重复或到达第二轮上限即停止。审核模型不接收其他席位的评分或观察；反馈仅交给Repair，Repair推荐也标为假设。

四头沿用五份有效均分4新增/2删除、IVT组件依赖保护。Phase七项评分必须各有五份有效意见；其他阶段唯一最高、均分≥4并超过当前阶段才替换。缺一份不补分。最终五头标签JSON不变，内部审核增加七个Phase候选，Repair使用已有五头提案Schema。

## 预先确定的收益标准

候选需同时满足：五头平均micro-F1高于同批A和H0；平均Precision、Verb F1和IVT F1不低于A；Phase不低于A和H0；相对A和H0均减少标签FP+FN。逐头退化、阶段改对/改坏、全五头正确率、每视频、候选覆盖、无效回答和费用全部报告。总体收益与Phase自身净收益分别解释；全部保留H0不算修复成功。

只在推理关闭、封存原始请求/回答、离线重放接纳后读取GT算分。工程故障保留原目录和费用，不覆盖、不自动重试。依据运行日志修工程问题；语义修改须先记诊断与假设，冻结后再请求。开发选出的方案需要固定的新Training目标确认；同视频确认仍不等于跨手术泛化，不使用Testing调参。

开发每次最多176请求（8×22），本次每次账户保护OpenRouter $3、阿里云¥2、xAI $0；原生费用、阿里估算、未知预留分别列账。先开展小样本开发再决定有证据的后续，不无限重复抽样追高分。

入口：`scripts/run_joint_phase_feedback_trial.py`；模板：`joint_phase_feedback_v1.txt`、`joint_phase_repair_v1.txt`。真实请求前用真实图片构造零API mock，验证两个轮次、五头Schema、失败回退和模型绑定。冻结入口用于执行和离线重放，避免其他对话更新活源码影响归档。

## 当前记录

开发v1：测试夹具的IVT组件写错，修正夹具，生产选择器不变，0 API。开发v2：冻结副本找不到凭据文件，调用前失败，0 API；修为只从原工作区读取指定凭据，快照不复制密钥。开发v3：完整执行170次，168次JSON解析成功、2次API失败，不重试、不剔除目标；原始请求离线重放通过，20项独立整数指标核验通过。全组对照共291.34秒。OpenRouter原生费用$0.29187870，另有未知预留$0.031172；阿里保守估算¥0.361998。

开发8目标F1（%，五头均值为各头micro-F1的算术平均）：

| 方案 | I | V | T | IVT | Phase | 五头均值 |
|---|---:|---:|---:|---:|---:|---:|
| 共享H0 | 95.65 | 72.00 | 61.54 | 37.04 | 100.00 | 73.25 |
| 当前五模型默认机制A | 91.67 | 64.52 | 59.26 | 38.71 | 87.50 | 68.33 |
| 联合第一轮B | 95.65 | 76.92 | 61.54 | 42.86 | 100.00 | 75.39 |
| 普通第二轮C | 91.67 | 76.92 | 64.00 | 44.44 | 100.00 | 75.41 |
| 定向第二轮D（开发后离线规则） | 95.65 | 80.00 | 64.00 | 44.44 | 100.00 | 76.82 |

B/C满足预先定义的总体收益条件，但此处H0的Phase本来8/8正确，B/C均未改Phase，不能称为Phase纠错提升。C相对B修对3项、改错2项，仅净减少1项标签错误。

有依据的唯一后续语义调整：开发记录显示，VID31/40251的第二轮新增verb_0、VID96/24776的第二轮新增instrument_0都不是Repair提出的修改，却由全池重新评分触发；两项均错误。VID23/13751的Repair明确提出删除null相关IVT及其V/T，审核通过后正确删除3项。因此新增D：第二轮只接受Repair完整提案与新一轮审核共同支持的修改；保留旧IVT组件保护，新IVT缺少获准组件则不加入。该规则不含帧号、GT、标签特例，不改阈值、提示词、模型或候选池，也不增加API。离线复用C回答，D相对B修对3项、改错0项；相对H0错误35→30，相对A错误45→30。它是开发集发现，不作为独立确认。

规则实现：`src/surgical_agent/research/verification/targeted_joint_repair.py`。开发离线证据：`artifacts/research/joint_phase_targeted_dev8_20260910_v1/metrics.json`。33项单元测试通过。

新16目标在开发GT评分前按元数据选定：四个Training视频各4帧，与历史抽样及彼此至少相距175原始帧。原Gemini H0和原图谱候选共32次调用已收集（137.30秒），尚未以确认标签调整规则。输入归档：`artifacts/preflight/joint_phase_fresh16_inputs_20260910_v1`。确认v1仅准备、0审核API；确认v2冻结D，计划SHA256 `48fedf68b79fcef1b7505b62b845b97539660019ae6c4650cb7330fe0f9f3024`；352次真实请求构造mock、0 API、0 GT读取通过，然后启动付费确认。A/B/C/D共享本批H0、图谱、图片和对应轮次原始回答。

### 第一次新16目标确认：未通过

16目标完成346调用、702.97秒；338正常JSON、1条去代码围栏后解析、1条因冲突的重复`image_indices`拒收、6次API失败（5次GPT独立Phase 403、1次GLM联合首轮503）。不重试，不剔除目标。原始回答重放通过，25项独立整数指标核验通过。OpenRouter原生$0.57472159，未知预留$0.09422625，阿里估算¥0.706215。新输入32调用另计OpenRouter原生$0.10609725。

| 方案 | I | V | T | IVT | Phase | 五头均值 |
|---|---:|---:|---:|---:|---:|---:|
| 共享H0 | 89.36 | 45.83 | 32.56 | 12.24 | 68.75 | 49.75 |
| 当前五模型默认A | 89.36 | 46.15 | 39.13 | 16.00 | 68.75 | 51.88 |
| 联合第一轮B | 89.36 | 45.83 | 36.36 | 12.24 | 68.75 | 50.51 |
| 普通第二轮C | 89.36 | 48.00 | 36.36 | 12.00 | 68.75 | 50.90 |
| 定向第二轮D | 89.36 | 45.83 | 36.36 | 12.24 | 68.75 | 50.51 |

B/C/D均未满足预先收益标准；D只挡误改，没有新纠错。Phase全保持H0（11/16），A阶段改对1个、改错1个；各组全五头完全正确均0/16。GT覆盖阶段1/2/3/5，仍缺0/4/6。剔除任何API/解析失败目标后的9目标诊断，以及对照调用完整的11目标诊断，同样是A优于联合方案，因此不能把失败仅归因于API故障。

候选覆盖：首轮正确IVT仅11/26，第二轮仍11/26；正确Verb覆盖17/25，Target覆盖12/22，都未改善。检查实际Repair请求，完整本体已提供，但当前预测、候选和大量重复反馈同时给入；15次Repair均重复当前完整五头预测。具体例子和逐帧审计在确认目录`targets/*/result.json`、`detailed_audit.json`，不是凭空推测缺定义。

### 第二次开发：独立图像重建Repair v2

将失败的16目标明确转为开发用途。只调整第二轮Repair输入：保留原三帧、完整本体、标签边界、阶段定义、待复查的任务名和同一JSON；移除当前答案、旧候选池、先前审核结论及图谱答案暗示。简短prompt要求逐器械尖端识别接触、动作和组织，再映射本体；持物不自动等于retract，邻近不自动等于操作。Verifier与v1逐字相同，所有模型、阈值和第一轮不变。此项检验的是整个Repair输入干预，不能区分去锚定与文字细节的独立贡献。

入口`run_joint_phase_continuation_trial.py`复用父归档的控制组和第一轮真实请求/回答，逐字核对请求，仅对有疑点的15目标新跑Repair+5审核，最多96、预计90新调用；收费账仅列新增调用。规则先冻结再跑，34项测试通过。归档`artifacts/preflight/joint_phase_independent_repair_dev16_20260910_v1`。若获得收益，仍需要另一批未看标签目标确认；本批不再称作独立验证。

该轮在90请求发出后被用户中断，15目标已完整保存，末目标3个审核回答未落盘。2026-09-10继续时没有存活Python进程。原目录保持不动；恢复到`joint_phase_independent_repair_dev16_20260910_recovered`：3条DISPATCHED改为INTERRUPTED_UNKNOWN，保留全部预留费用，不重试、不补票；原15目标逐字重放一致，末目标缺票回退。恢复0 API、0 GT；随后16目标完整评分、25项独立指标核验。85次正常JSON、2次安全解析拒收、3次中断未知。新增OpenRouter原生$0.199675168、未知$0.05527481；阿里估算¥0.249304、未知¥0.148255。记录的时间仅为API时间戳跨度下界，不是完整墙钟耗时。

独立Repair v2普通第二轮：I 91.67、V 50.00、T 36.36、IVT 12.50、Phase 75.00，均值53.11；定向第二轮：I 91.67、V 46.81、T 36.36、IVT 12.50、Phase 75.00，均值52.47。两者均超过A均值51.88，但IVT低于A的16.00，故预注册完整收益条件仍未通过。Phase改对1、错阶段换成另一错阶段1、没有改坏原本正确Phase。正确I/V/T候选覆盖分别增加1项；正确IVT覆盖仍11/26。完整头正确仍0/16。

### 第三次开发：第二轮Verifier去答案提示v3

依据：v2已让部分Repair不再照抄、增加候选，也带来实际Phase纠错；但审核请求仍明确给出当前完整答案及Repair阶段答案，部分候选未获接纳。不能直接证明这是锚定效应，因此做单独机制对照：仅从第二轮Verifier包删除`current_prediction_hypothesis`、`phase_recommendation_hypothesis`；三帧、池、评分prompt、Schema、五模型和阈值逐项相同。第一轮不改；v2 Repair全部复用。原回答缺失的第二轮是新机制下重新审核，不是为旧机制补票。75次真实请求mock和271次父请求重放通过；36项测试通过。归档`artifacts/preflight/joint_phase_blind_review_dev16_20260910_v1`；最多80、实际预期75新调用。

后续确认样本在v2开发评分前已按元数据预选。30秒间隔（750原始帧）因VID103不足失败；14秒（350帧）因VID96不足失败，均0 API。最终沿用大于175原始帧的间隔成功选出新16目标，归档`joint_phase_independent_fresh16_inputs_20260910_v3`，截至第三次开发启动仅prepare、0 API、未读取标签评分。所有确认仍来自相同四个Training视频，不代表跨视频泛化。

v3在45请求后随对话切换停止，9目标完整，第10目标仅GLM未落盘。原目录保留，续跑`joint_phase_blind_review_dev16_20260910_resumed`复用44条已保存回答、保留1条中断未知，只发余下30条请求，共75条。后台运行后16目标完成；又有2次Qwen SSLError，未降低TLS校验、未重试。后续无鉴权GET连接探测成功（HTTP404，0模型调用）。完整重放及25项整数指标核验通过。

v3普通第二轮：I85.11、V45.83、T39.13、IVT8.33、Phase68.75，均值49.43；定向第二轮均值47.47，Phase62.50。普通版Phase改对1、改坏1；定向版改坏1。即使只看所有相关调用完整的7目标，v3也没有优于对照。本轮不支持去除Verifier答案提示，撤回该改动。中断与SSL意味着不能把所有差异都纯粹归因于语义机制。OpenRouter原生$0.16109429、未知$0.01605880；阿里估算¥0.225181、未知¥0.305085。原目录和续跑目录是同一逻辑实验，费用不可重复相加。

### 第四次开发：Repair保留有限歧义候选v4

恢复原v1 Verifier（保留当前答案及Phase推荐、全部阈值不变）。只修改独立Repair的文字：有歧义时每个可见器械可提出最佳IVT及最多一个合理替代；输出仍为既有完整五头JSON，I/V/T由候选组件组成，Phase仍一个ID。明确这些是交给审核的竞争假设，不是同时成立的最终标签。依据是v2正确IVT候选覆盖仍11/26，过早选定单一动作/组织解释使审核无从补救；不指定GT标签、帧号或强制新增。

38项测试通过；90条新请求mock与256条原第一轮/控制请求重放通过。计划SHA256 `a9983c22be0ae299e38d8ec4ab38dbf983acb5b9704614143ae2bdc21b4ed275`。归档`joint_phase_alternative_repair_dev16_20260910_v1`，16目标90新调用已全部完成，正在离线评分。无默认版本变更。

v4已完成离线重放与25项独立指标核验。普通第二轮I91.67、V53.06、T39.13、IVT12.24、Phase68.75，均值52.97；定向版V50.00、均值52.36。相对A总体和Verb提高，但IVT仍低于16.00，完整收益条件未通过。正确IVT候选覆盖11/26→15/26，未转化为新增正确IVT接纳，故不能把候选覆盖改善写成最终识别提升。OpenRouter原生$0.220123847、未知$0.01692760；阿里估算¥0.270161。

### 第二批新16目标：冻结v2主方案及v4次方案确认

选v2普通第二轮为主方案（开发均值最高），v4普通第二轮为次方案（开发Verb最高），定向接纳继续仅作对照；两版均未通过开发阶段完整收益标准，故不能提前宣布完成。新16目标的32次上游调用已收集；同一H0、原图谱候选、图片、控制组和第一轮将在两版共享。确认前停止一切语义调参，完整收益规则不变，也分别报告单头收益/退化。

调度改为每次两个独立目标并行，单目标内部步骤不变，结果按冻结选择顺序保存。`run_joint_phase_parallel_trial.py`的352次并发真实请求构造mock、0 API、0 GT检查通过。主方案`joint_phase_confirm16_independent_20260910_v1`已启动；完成后先冻结v4次方案，再读这批GT评分，避免确认标签影响次方案。两版结果全部报告。此调度只减少等待时间，未改变模型、prompt、候选、评分或接纳机制。

## 最终确认结果与采用建议

两版均已完成，未再修改prompt或阈值。主方案340调用、395.25秒；次方案复用第一轮，仅新增84调用、189.79秒。两个独立目标并行；这两个时间包含各自对照/复用开销，不是某个单一生产分支的逐帧时延。主方案332正常JSON、2去围栏后解析、4安全解析拒收、2 API失败；次方案83正常JSON、1安全解析拒收。所有16目标保留，没有重试或挑选成功回答。两版均通过原始回答重放、共50项独立整数指标核验、模型及账本检查。

以下全部来自第二批新16目标，F1为百分比。总错误是五头标签FP+FN之和。

| 方案 | I | V | T | IVT | Phase | 五头均分 | 总错误 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 共享历史H0 | 88.89 | 65.31 | 44.44 | 36.00 | 75.00 | 61.93 | 87 |
| 当前默认机制A（同批五模型重跑） | 86.36 | 62.96 | 50.00 | 32.73 | 81.25 | 62.66 | 93 |
| 联合第一轮B | 88.89 | 72.34 | 50.00 | 34.04 | 81.25 | 65.30 | 77 |
| 独立Repair v2普通第二轮 | 86.36 | 69.39 | 50.00 | 33.33 | 81.25 | 64.07 | 81 |
| 独立Repair v2定向第二轮 | 88.89 | 70.83 | 50.00 | 34.04 | 81.25 | 65.00 | 78 |
| 替代候选Repair v4第二轮（普通与定向相同） | 88.89 | 70.83 | 50.00 | 36.73 | 81.25 | 65.54 | 78 |

本批所有联合方案满足事先定义的相对H0/控制组总体收益条件。平均F1最高的是v4第二轮，相对A提高2.88个百分点，Verb提高7.87、IVT提高4.01，Phase持平；相对A修对18项、改错3项，净减少15项错误。相对H0修对15项、改错6项，净减少9项错误。四视频分组相对A均值均提高，但VID23的v4均值55.89低于其H0的63.89，因此不能宣称每个视频均优于H0。

**第二轮增量不应夸大。** 第一轮B已相对A净减少16项错误。v4第二轮相对B只修对1项、改错2项，总错误77→78；平均F1却65.30→65.54，因为不同头的F1分母不同。Verb72.34→70.83、IVT34.04→36.73。全五头完全正确的目标从B的1/16变成v4的0/16（A为0/16，H0为1/16）。具体是VID103/12926补对IVT7，VID23/15226误加verb0及IVT7；这些都保留在逐帧记录中。v2普通第二轮相对B只引入4项错误，定向规则挡住其中3项，仍没有新增纠错。

因此，本次建议把**联合第一轮B作为优先扩大验证的候选**：实际净纠错更多、Verb更高、少一次Repair和五模型复审。v4第二轮保留为IVT/平均F1更高的实验选项，等待Gate或另行验证的触发策略决定何时调用。此处是实验建议，不是自动更换默认。默认仍为`parallel-phase-repair-v1.3.0-glm-low`；未训练Gate，未修改默认H0、图谱或五模型配置。

Phase方面，B与所有后续联合版本相对H0均改对1个、没有改坏原本正确阶段；A改对2个、改坏1个。因此81.25%的阶段准确率相同，但错误分布不同；第二轮没有新增Phase纠错。阶段覆盖仍只有1/2/3/5，未覆盖0/4/6。所有调用完整的11目标诊断中，v4均值66.91高于A62.51；控制调用完整的13目标中，v4为67.14、A63.10。收益不是只来自控制组调用失败。上述子集仅作诊断，不替代完整16目标结果。

候选覆盖方面，v4正确IVT从16/26→18/26，仍有8项正确IVT在池外。数量限制只写在prompt中，未改变JSON Schema：开发集每类器械最多2条；新确认的VID31/66576返回了3条hook候选（58/59/96），违反期望的软上限，未事后裁剪，该目标最终输出未改变。因此不能把当前实现描述为代码强制的“最多2候选”。

Repair输入平均token在同一开发16目标、15次Repair上：原v1为6700.20，独立v2为4240.73，替代v4为4248.73。约减少37%，仅指Repair输入；v4扩大候选会增加后续Verifier负担，不能外推整条pipeline也省37%。最终五头输出JSON及Repair/Verifier已有Schema保持一致。

## 归档、复现与费用

- 主确认：`artifacts/preflight/joint_phase_confirm16_independent_20260910_v1`。
- 次确认：`artifacts/preflight/joint_phase_confirm16_alternative_20260910_v1`。
- 每个目录包含冻结源码、plan、初始状态、原始请求/回答、账本、预测、GT评分、逐帧修对/改错、独立审计及逐视频诊断。评分使用各自`frozen_source/scripts/run_joint_phase_parallel_trial.py score --output <该目录>`，无需API。
- 全系列机读索引：`artifacts/research/joint_phase_feedback_series_20260910.json`。同一实验原目录与恢复副本仅计费一次。
- 本次累计40个不同目标、1259次实际请求。OpenRouter原生费用合计$2.410313395；阿里保守估算¥2.797264。另有未知预留OpenRouter $0.24483066、阿里¥0.453340，不能当作已确认账单。缓存重放、mock和中断恢复均未新增模型调用。
- 38项单元测试通过；并发真实请求构造预检通过；主/次确认分别25项独立指标核验通过。全部失败试验保留，无默认版本自动晋升。

结论适用于同四个Training视频的这批新目标。第一批新16目标曾失败，随后被用于开发；第二批新16目标在调整前按元数据固定，作为本次最终确认。样本量小，视频相同，不能宣称已经证明全训练集或跨视频的稳定收益。
