# 五模型平均评分与三轮修复：开发小测试

> 2026-09-08 文档发布说明：本页保留实验结束时的结果及 Git 状态；“未提交”等表述不是本次发布状态。完整机制索引与当前发布范围见 [汇总](VERIFIER_REPAIR_EXPERIMENT_SUMMARY_2026-09-08.md)。近期本地脚本、冻结源码与原始实验目录未随这次文档提交发布，文中相关复现命令需要本地产物。

状态：实现、真实五模型兼容性冒烟、部分目标实测及独立审计完成。计划8目标，实际发送6目标，另外2目标因未知费用预留与预算限制未发送；没有完整有效的第二轮或真实第三轮，没有接纳修复。不能将本轮称为完成8目标的三轮实验，也不能据此断言五模型平均分的语义效果好或坏。默认H0保持不变。

## 核实后的问题

旧版 [prior_panel.py](../src/surgical_agent/research/verification/prior_panel.py) 的 `build_universe` 只包含 H0 标签、7类器械和少量先验候选；`issues_for` 只能发现池内问题。`panel_votes` 要求3人回答 PRESENT/ABSENT/UNCLEAR，`admit` 用新增2票、删除3票，完全没有1–5评分或取平均。`run_arm` 在无新问题、重复修订稿等条件下停止，之前真实实验没有R3。

旧8目标实验还有视觉层面的失败：有完整初审的5个真实新增IVT没有任何原始PRESENT，其中3个被三人一致判为ABSENT。另有时序引用规范化、输出截断、服务商拦截。候选覆盖、视觉判断、接纳规则和接口失败是不同问题，不能用JSON合法率代替语义效果。

## 本版如何工作

入口 [run_mean_panel_trial.py](../scripts/run_mean_panel_trial.py)，纯逻辑 [mean_panel.py](../src/surgical_agent/research/verification/mean_panel.py)。默认纯API入口和冻结H0保持原样。

1. 复用原8个Training目标的真实H0，不再调用初始预测。它们已被分析过，因此这是开发重放，不是独立验证集结果。
2. 五个轻量视觉模型各自看同一因果三帧，预测全部132个标签的存在程度：I7、V10、T15、IVT100。每项1–5整数；1明确不存在，2倾向不存在，3不确定，4倾向存在，5明确存在。每个模型评分4/5的标签即该模型的正类预测。Phase保持H0。
3. Python对每个标签取五人算术平均。均分≥4支持存在，≤2支持不存在，中间不产生修改。不是整帧总分，不是置信概率，也没有将三态投票伪装为数值平均。
4. 均分与当前已接纳预测冲突时，Qwen根据具体问题和图像提出最多16项局部补丁；没有独立候选H1/定位器调用。所有新增组件需要自己的问题项；删除IVT不自动删组件。
5. 修订稿必须再由五人完整审核。接纳只依据本轮均分，重新审核相对原H0的所有累计修改。新IVT的缺失组件也需支持；保留IVT需要的组件不能被删除。未确认修改回到H0，不用旧轮的高分兜底。
6. 最多3轮五模型审核、2次Qwen修复。初轮不向五人展示H0；后轮展示修订稿和该成员自己的前次回答，不展示其他成员分数。无可执行问题、空/非法补丁、失败审核或预算不足会提前停止；重复问题本身不再强制早停。中间分数代表未解决，不代表审核通过。

缺一位成员就不计算五人均分，保留最后完整审核过的状态。错误响应不自动重试，不增加第四轮。各轮前缀是同一次执行的相关结果，不能用GT选最好轮次。

## 医疗学术场景说明与接口检查

按用户要求，五位成员的系统提示和输入JSON第一项 `academic_context` 均说明：输入是用于学术医疗视频分析的腹腔镜手术帧，研究任务是标注器械、动作与交互组织；组织、血液和器械属于所研究的医疗过程。Repair也带相同说明。原H0提示不变。

这是真实场景说明，不代表服务商保证放行。v1中Flash-Lite返回完整132项评分，GPT-4.1 mini仍返回403 `violence/graphic`，响应明确注明未扣费。五人冒烟未通过，正式实验没有启动。原始响应与冻结源代码保存在 `artifacts/preflight/h0_five_mean_openrouter_20260907_v1/`。

正式预测前，v2将两个OpenAI评分席位换为Qwen3-VL8B和Gemma3-12B；Flash-Lite和Qwen-VL通过，但Claude Haiku也返回403 `violence/graphic`。v3将Claude席位替换为Mistral Small3.2-24B，固定图片、医疗说明、评分与循环规则，此后本任务不再继续换模型试探。最终五个候选成员：Gemini2.5 Flash-Lite、Qwen3-VL8B Instruct、Mistral Small3.2-24B、Gemini2.5 Flash、Gemma3-12B。Qwen3.8-max-0902仍只负责修复。模型端点兼容性通过OpenRouter公开端点元数据检查；元数据支持不等于真实冒烟通过。Qwen-VL不支持reasoning参数，因此不向它发送此参数；Qwen修复保留原low设置。所有旧版源代码与请求都在各自输出目录中冻结。

## 实验与费用边界

原8目标顺序不变：VID103/18426、33676；VID23/13076、27726；VID31/40801、73601；VID96/14876、25876。I/P8个有效GT，V/T/IVT7个；不因缺GT或接口失败换样。Testing不参与。

本次五模型方案总调用上限142（包含兼容性尝试）。每个目标最多15次评分+2次修复，兼容性冒烟6次；失败尝试也扣调用数。因此上限并不保证全部目标跑满三轮。原15美元累计预算继续扣除先前三模型实验费用与未知成本预留；v1新增两次尝试后，v2保守可用11.16002414美元、剩余140次请求；v2又尝试3次后，v3保守可用10.676120026美元、剩余137次。未知成本继续按完整预留计入，即使文字响应声称未收费，也不会悄悄当作费用凭证齐全。

这是同时更换评分方式、成员、候选范围和反馈机制的可行性测试，不能归因成仅增加两个模型的效果。暂不混入先验频率、Tracker、RAG或额外记忆。全本体覆盖解决候选池的程序限制，不保证模型能看对；自我复核重复使用相同图片，没有增加外部证据。

## 复现与验证

使用现有 `requirements-prior-panel.txt`。先prepare冻结代码、输入和价格；smoke通过后才能run；保存预测完成哈希后才能score。

```powershell
.venv-p2/Scripts/python.exe -m pytest tests/unit/test_mean_panel.py tests/unit/test_prior_panel.py tests/integration/test_prior_panel_budget.py -q
.venv-p2/Scripts/python.exe scripts/run_mean_panel_trial.py prepare --output artifacts/preflight/NEW_UNIQUE_RUN
.venv-p2/Scripts/python.exe scripts/run_mean_panel_trial.py smoke --output artifacts/preflight/NEW_UNIQUE_RUN
.venv-p2/Scripts/python.exe scripts/run_mean_panel_trial.py run --output artifacts/preflight/NEW_UNIQUE_RUN
.venv-p2/Scripts/python.exe scripts/run_mean_panel_trial.py score --output artifacts/preflight/NEW_UNIQUE_RUN
.venv-p2/Scripts/python.exe tools/audit/audit_mean_panel.py artifacts/preflight/NEW_UNIQUE_RUN
```

新机制11项测试及独立的模型参数兼容性测试通过；与冻结H0、final-only、独立评分和原panel账本相关回归共68项通过。测试覆盖恰好五人、平均分边界、评分类型/长度、完整本体、共享组件、跨轮重新判断、第三轮失败回退、Qwen-VL不发送不支持的reasoning参数，以及五模型实际请求的医疗说明位置。5个新Python文件Ruff通过。本轮未执行提交或推送命令；实际仓库状态见文末核验。

## 实际执行和停止原因

v3的五个模型都在真实冒烟中返回完整132项评分，Repair空问题检查也通过。正式测试第3个目标发生Google503超时后停止。v4只续跑尚未发送的目标：核对模型/协议/核心代码/请求构造函数一致，复用v3冒烟，保留前三个已尝试目标的原结果，不重试失败请求。v3暂停时曾保存和评分包含占位回退的8行文件；v4未查看这些GT来改参数、候选或选样，最终审计只在v4完成记录后执行。

v4在第6个目标遇到Flash-Lite与Flash均返回HTTP200但正文为 `Gemini blocked the response: OTHER`，没有费用明细。两次请求都计失败，不能把HTTP200当成功。此后保守余额3.270204562美元，低于完整五人一轮预留3.971331904美元，因此未再续跑最后两个目标。**这不是实际花完了15美元，而是当前保守费用预留规则限制了继续发送。** 未为赶完实验清空未知负债或重置预算。

- 真正发送过6个目标的首轮五人审核；其中3个首轮完整有效。
- 3次Repair调用：一次给出5项合法提案，一次空补丁，一次4项提案中有2项不绑定各自的问题，整包拒绝。
- 只有上述合法提案进入第二轮五人审核；该轮因一位成员格式失败而回退。
- 第二轮请求过1组，但完整有效第二轮为0；P2与R3均没有真实请求。
- 6个实际尝试目标都保持H0，另外2个未发送目标保存H0占位。改对、改坏、部分改善、无收益替换的最终接纳数均为0；这不代表已证明安全有效。

正式模型/格式失败：Gemini Flash一次IVT数组返回101项而非100项；Flash-Lite一次观察文本2474字符而非≤1000；Google一次503超时；另一个目标的两个Google成员返回blocked OTHER。观察文字失败使整份数字评分不能参与本轮均分，这是本版严格契约的实际代价。没有擅自截断错位数组、放宽1000上限或用4位成员代替5位。

Repair的越界提案不是ID格式问题：它收到两个REMOVE问题，却额外提出两个ADD，并引用那两个REMOVE的问题作为新增依据；程序按“修改必须绑定自身问题”拒绝整包。因此单纯增加评分模型，不能保证Repair遵守逐项修改约束。

## 分数与修改诊断

以下仅为**实际尝试的6个目标，回退后的H0与最终输出完全相同**，不能与此前8目标的24% IVT F1直接比较。单元格为micro-F1 / 集合Accuracy（%）。

| 任务 | 有效GT | 同批H0 | 最终回退结果 |
| --- | ---: | ---: | ---: |
| Instrument | 6 | 95.24 / 83.33 | 95.24 / 83.33 |
| Verb | 5 | 55.56 / 40.00 | 55.56 / 40.00 |
| Target | 5 | 63.16 / 20.00 | 63.16 / 20.00 |
| IVT | 5 | 31.58 / 20.00 | 31.58 / 20.00 |
| Phase | 6 | 83.33 / 83.33 | 83.33 / 83.33 |

保留计划8行（含2个未发送占位）计算时，H0和终态也完全相同：I96.30/87.50，V58.33/42.86，T56.00/14.29，IVT24.00/14.29，Phase87.50/87.50。这组数用于核对保存结果，不是8个目标都完成五模型实验的证据。

事后按任务mask检查原始修复提案：合法的5项补丁中4项有益、1项有害；另一个被拒绝的4项补丁全部有害，其中2项没有自己的问题依据；空补丁没有修改。合计原始提案4项有益、5项有害，**全部未被最终接纳**。合法补丁的复核失败源于文字长度，不是成功识别有害修改，因此不能宣称Verifier主动拦住了这些错误。原始无效提案不是有效H1，更不是模型终态指标。

## 费用、审计与产物

本次五模型方案累计49次POST：兼容性11次、正式评分35次、Repair3次。已结算 **0.185653578美元**；5次失败没有逐笔usage.cost，保留 **8.1509552美元** 的完整上界预留。两个403响应虽明确说明未扣费，仍保留原始账本负债以便后续对账，不伪称已有逐笔凭据。

账户usage从12.709956985变为12.895610563，增量恰好0.185653578美元，没有未归属扣费差额。账户一致不能补出每次失败的独立账单，预留不是已经扣费。连同上一组三模型实验，本次授权累计已结算1.032043438美元、未知预留10.697752美元，占用11.729795438美元，仍低于15美元。

独立审计完整重放所有目标的接纳/回退，核对原H0未变、最多三轮、实际请求的医疗说明位置、各头TP/FP/FN与集合Accuracy、修改计数、费用和失败调用。审计通过；这验证的是记录和实现一致，不是视觉答案准确。

以下原始产物保留在本地 `artifacts/preflight/h0_five_mean_openrouter_20260907_v4/`，未随文档上传；公开计数摘录见 [指标快照](experiments/verifier_repair_results_20260908.json)。

- 最终合并输出：`predictions.json`
- 独立审计与费用：`independent_audit.json`
- 机制诊断和实际 6 目标指标：`mechanism_diagnosis.json`
- 账户对账：`account_reconciliation.json`
- 冻结执行计划：`plan.json`

结论：用户要求的五模型真实评分、取平均、条件修复已实现并部分运行；医疗学术说明已生效于请求文本，但没有证明它降低拦截率。当前优先修的是数字输出契约（例如使用明确ID键避免数组错位）、辅助文字与评分的失败边界、Repair对问题清单的越界行为，再考虑相同规模的新冻结测试。上述是下一步建议，本次没有据此改动已跑协议或追加付费实验。暂不新增模型、反思轮数、Tracker或RAG，不升级默认H0。

## 最后Git核验

收尾时发现本地HEAD已是 `7a44312477b9415c34c6f307ef943e9f087d9dff`，提交标题为 `Add verifier review and bounded multi-model repair experiments`；`git ls-remote origin refs/heads/main`核实远端main也是该提交。本轮没有运行git commit/push，不能将此仓库状态归因为本轮执行了发布。五模型脚本、核心和测试已被当前HEAD包含；本报告与docs/README还有本地补充，因此GitHub中的报告不包含这些最后补充。默认H0五个关键文件相对用户最初指定的 `25fee9485a8f740aadf7921d5312e9c488955cbb` 没有差异。保持现有提交与本地修改，没有回退或清理它们。
