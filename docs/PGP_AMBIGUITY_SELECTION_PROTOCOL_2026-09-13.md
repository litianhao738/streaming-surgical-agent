# Offline research default selection, fixed before reassessment fitting

Preserve every prior artifact and snapshot the current research default. No Tracker inputs, API, Testing, VID110, deployment or deletion. Use only the sealed 6059 original Training rows. This is conditional development evaluation on reused videos, not independent validation.

Freeze Claude's ambiguity rollback policy, single compact Qwen probe, ten probe features and 32 non-Tracker cheap features. HistGradientBoosting is the primary model with its existing fixed parameters; base HGB and Qwen logistic regression are descriptive ablations. No hyperparameter search or choice of a new model from the ablations. Independently refit outer/inner video LOVO; select thresholds from the 20 fixed inner score quantiles under the declared retention90 criterion. Preserve the original full-reference inner criterion as a diagnostic control using the same fitted scores. Do not round F1 before comparing. Rank feasible thresholds by total logical cost, then fixed index. Store every candidate and every score/route. Fit final model from scratch after selecting its threshold on outer OOF scores; never report that calibration as independent performance.

Research default eligibility: all inner folds feasible, outer original pooled F1 >= max(cheap, original full), errors <= min(cheap, original full), each video no worse than cheap in F1/errors, actual improvement, and fewer calls AND known USD than the same ambiguity repair with all frames probed and continued. Also require schema/serialization/replay consistency and intact sources. If the primary candidate is eligible and dominates the existing PGP quality (no fewer than its F1 and no more errors), select it as the offline research default. Otherwise retain the existing default. This selection accepts extra cost versus old PGP only to recover full-verification quality, consistent with the user's stated purpose.

Separately assess outer gain retention >=90%. Failure prevents a claim of complete stability or deployment readiness, even if the original quality/cost criteria qualify the model as the best research default. Record both statuses, never substitute one for the other. Final all-Training OOF calibration must be feasible before selecting a default artifact.

Cost accounting uses the same original per-call ledger as the existing PGP. Charge an already queried Qwen even if compact stopping depth is zero. Charge proposal for every probed row; phase recommendation and remaining compact/joint requests only on continuation. Do not infer USD from rounded constants. Report GLM/DeepSeek separately. Compare cheap, original full, ambiguity full with and without mandatory Qwen, existing PGP, net-gain PGP, and fixed-seed 300-draw per-video call-cap random routing (which always pays the same Qwen prefix).

Existing Claude preparation functions may be reused as reviewed replay code, but execute no top-level fits or output writes from its script. Independent fitting, exact-count evaluation and explicit request-key accounting are implemented in the new runner. Frozen code hashes document this dependency. Removing 11 all-zero Tracker columns should not materially change predictions but exact equality is not assumed.
