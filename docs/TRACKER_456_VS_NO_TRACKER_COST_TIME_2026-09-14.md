# 方案 4、5、6 与原无 Tracker 的精度、时间和费用比较

本次补算零 API。分别列出相同样本的历史参考、同次回复的消融对照，以及实际实验投入。不能把 6059 帧与 16 帧直接比较，也不能把本地缓存回放耗时当成在线推理速度。

## 6059 帧完整开发回放

| 版本 | 五头 F1（%） | 错漏 | 逻辑调用 | OpenRouter 估计 | Qwen 估计 | GLM 请求 | DeepSeek 请求 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 原无 Tracker PGP | 61.6989 | 36150 | 37157 | $83.6543 | ¥19.5535 | 1995 | 1154 |
| 方案 4，旧 Gate | 66.3858 | 32423 | 26647 | $58.8699 | ¥11.3350 | 1696 | 1095 |
| 方案 4，Gate v2 外层 | 66.3849 | 32427 | 24047 | $54.4045 | ¥11.3350 | 1357 | 907 |

账户估计依据历史各 stage/seat 平均结算成本按该路径的调用重算。Qwen 单独以人民币列出，GLM/DeepSeek 只有请求数，不编造货币金额。此前 known_usd_estimate 包含 Qwen 按情景汇率折算，本表改为账户分列，因此不可将两个 USD 字段混用。

方案 4 完整版相对原无 Tracker F1 +4.6860 点，调用 -35.28%，OpenRouter 估计费用约 -34.96%。这些收益包括阶段滤波和动作规则，并不全是 Tracker 的独立贡献。

## 与方案 5、6 相同的 16 帧

| 版本 | F1（%） | 错漏 | 逻辑调用 | OpenRouter | Qwen | GLM | DeepSeek |
|---|---:|---:|---:|---:|---:|---:|---:|
| 原无 Tracker，历史外层缓存参考 | 62.7000 | 96 | 121 | $0.27304 | ¥0.06319 | 5 | 4 |
| 方案 4，Gate v2 历史外层缓存 | 66.9587 | 86 | 65 | $0.14474 | ¥0.02993 | 4 | 3 |
| 方案 5，在方案 4 后处理上增加先验提示 | 66.5470 | 88 | 82 | $0.17042 | ¥0.02160 | 7 | 5 |
| 方案 6，在方案 4 后处理上增加 Qwen 提示 | 65.5712 | 87 | 73 | $0.15851 | ¥0.02450 | 4 | 3 |

前两行采用历史原响应和外层 Gate；后两行采用本轮新响应和旧全量 Gate 作迁移对照。样本相同，但响应实现和 Gate 标定不同，因此只能作为原流程参考比较，不能据此宣称方案 5/6 的提示带来表中全部差值。后两行费用为本轮已采集回复对应的虚拟推理路径成本，包含共享历史 H0；前两行为历史均价估计。未重新付费采集原完整流程。

更严格地固定本轮输入回复、旧 Gate、仅交互审核及同一阶段滤波，去掉 M1/M2 Tracker 后处理的对照如下：

| 本轮同条件比较 | F1（%） | 错漏 |
|---|---:|---:|
| 无 Tracker，保留相同阶段滤波 | 64.3877 | 92 |
| 方案 4 后处理，旧 Gate | 66.1444 | 86 |
| 方案 5 | 66.5470 | 88 |
| 方案 6 | 65.5712 | 87 |

此处无 Tracker 对照与方案 4 的逻辑调用相同（76），API 路径成本相同（OpenRouter $0.161896、Qwen ¥0.022644、GLM 4、DeepSeek 3），因为 M1/M2 是本地输出处理。

方案 5 唯一实际改变先验提示的帧最终结果不变，整体差异来自其他同提示重采帧，不能归为 Tracker 先验收益。方案 6 相对本轮方案 4 多漏一个 target。两者都没有通过预设质量检查。

## 实际实验耗时和新增采集费用

| 工作 | 实测耗时 | 新请求 | 本轮账户投入 |
|---|---:|---:|---|
| 方案 4 本地完整回放、逐帧核验及拟合 | 169.02 秒，约 2 分 49 秒 | 0 | 新 API 费用 0 |
| 方案 5，包含重新采集配对对照 | 746.69 秒，约 12 分 27 秒 | 192 | OpenRouter $0.234047；Qwen ¥0.044249；GLM/DeepSeek 各 32 次 |
| 方案 6 增量采集，复用上述对照 | 147.45 秒，约 2 分 27 秒 | 16 | Qwen ¥0.024496 |
| 方案 5、6 合计 | 894.57 秒，约 14 分 55 秒 | 208 | OpenRouter $0.234047；Qwen ¥0.068745；GLM/DeepSeek 各 32 次 |

在线采集时间从第一请求发出到最后回复结束；不含代码开发、准备和后续评分。方案 6 较短是因为实验复用了其他回复，不能解释为完整方案 6 在线推理只需该时间。

完整在线端到端耗时尚未实测，不能用逻辑调用降幅直接等比例换算加速。当前费用也未包括 Tracker 检测/跟踪 GPU 时间、功耗及本地训练成本。方案 4 的阶段滤波另有观测延迟中位数 11 秒和 3 个未命中的阶段片段，详见原方案 4 报告。

计算产物：`artifacts/research/tracker_schemes56_pilot_20260914_r5/baseline_comparison.json`。
补算脚本：`tools/audit/compare_tracker_456_baseline.py`。

当前结论仍是优先方案 4；方案 5、6 没有额外精度收益证据，暂不加入默认流程。所有数字均为重复使用的 Training 开发证据，未覆盖独立测试。
