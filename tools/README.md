# Project Tools

- `dataset/download_cholectrack20.py`: Synapse downloader. Credentials are
  supplied through environment variables; the raw dataset remains external.
- `audit/audit_cholectrack20.py`: read-only dataset audit that writes reports.
