# Single dependency source for the pipeline, Tracker training and validation tools.
# Python >=3.10,<3.13. No environment packages were changed when merging this file.

# Preserve compatibility with the fitted Gate estimator.
numpy==2.4.6
scikit-learn==1.9.0
joblib==1.5.3

# Core inference, reports, dataset access and image processing.
PyYAML>=6.0,<7
Pillow>=10.0,<13
opencv-python-headless>=4.8,<5
requests>=2.31,<3
scipy>=1.12,<2
synapseclient>=4.7
jsonschema==4.25.1
ivtmetrics==0.1.5
tqdm>=4.66,<5

# Tracker/ECDet training support from the existing profiles.
# Keep a matching CUDA torch/torchvision pair on GPU hosts.
torch>=2.6,<3
torchvision>=0.16,<1
pycocotools>=2.0.10,<3
tensorboard>=2.18,<3
matplotlib>=3.9,<4
calflops==0.3.2
transformers>=4.40,<5

# Local tests and linting.
pytest>=7.4,<10
ruff>=0.6,<1
