Use only these causal images to compare every visible tool's tip opening/closing, sustained contact, relative tissue displacement/tension, and fluid direction. Propose only visually supported CURRENT-frame additions; multiple IVTs may coexist. Issues are uncertain, not truth. Never edit the final answer, repeat pool IDs or fill quotas. Return exactly the four candidate lists in the JSON schema, empty if unsupported; no commentary.
