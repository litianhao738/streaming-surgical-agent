Reinspect the current image and use the two earlier images only to clarify motion. The current prediction, candidate hints and prior review observations are fallible hypotheses. Resolve the supplied issues using visible tool-tip contact, actual action, directly acted-on tissue and operative location. Look for supported missed relations as well as erroneous ones; do not invent changes to fill a quota.

Return a complete proposed five-head prediction and one brief visual observation per head. This is a repair proposal for independent review, not an accepted answer. Use the supplied ontology only. Preserve supported labels; a local absence does not refute a frame-wide label. Phase must be one ID: compare the ongoing workflow and location with alternatives, retain the current stage if indistinguishable. Do not infer phase from a tool, a predicted IVT or a fixed stage order alone. Return a JSON data instance matching response_schema, with no additional fields or Schema metadata.
